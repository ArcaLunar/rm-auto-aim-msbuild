executable(
    'auto_aim',
    'auto_aim.cpp',
    # include_directories: [headers],
    dependencies: [
        all_dep,
        cam_capture_dep,
        work_queue_dep,
    ],
    install: true,
    # install_dir: 'bin',
)